import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ProductsPageGrid = styled(Grid)(() => ({
  backgroundColor: "#C2A7DB",
  height: "100%",
  padding: "15px",
}));
