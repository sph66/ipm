import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";

export const DebtPageGrid = styled(Grid)(() => ({
  backgroundColor: "#bbdefb",
  height: "100%",
  padding: "20px",
}));
